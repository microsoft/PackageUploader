{
  "operationName": "RemovePackages",

  "productId": "{{ productId or bigId required }}",
  "bigId": "{{ productId or bigId required }}",

  "flightName": "{{ flightName or branchFriendlyName required }}",
  "branchFriendlyName": "{{ flightName or branchFriendlyName required }}",

  "marketGroupId": "{{ optional - if informed it will only delete packages in the specified one }}",

  "aadAuthInfo": {
    "tenantId": "{{ required }}",
    "clientId": "{{ required }}",
    "clientSecret": "{{ XBOX RECOMMENDS using the command line parameter --ClientSecret instead }}"
  }
}
