{
  "operationName": "UploadUwpPackage",

  "productId": "{{ productId or bigId required }}",
  "bigId": "{{ productId or bigId required }}",

  "flightName": "{{ flightName or branchFriendlyName required }}",
  "branchFriendlyName": "{{ flightName or branchFriendlyName required }}",

  "marketGroupId": "{{ default value is 'default' }}",

  "packageFilePath": "{{ required }}",
  "minutesToWaitForProcessing": 60,

  "availabilityDateConfig": {
    "availabilityDate": "{{ if blank it will disable the availability date in Partner Center for this branch }}"
  },

  "mandatoryDateConfig": {
    "mandatoryDate": "{{ if blank it will disable the mandatory date in Partner Center for this branch }}"
  },

  "aadAuthInfo": {
    "tenantId": "{{ required }}",
    "clientId": "{{ required }}"
  }
}
