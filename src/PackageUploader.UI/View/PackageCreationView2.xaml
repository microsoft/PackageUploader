<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:PackageUploader.UI.ViewModel"
             xmlns:controls="clr-namespace:PackageUploader.UI.Controls"
             x:Class="PackageUploader.UI.View.PackageCreationView2"
             x:DataType="viewmodel:PackageCreationViewModel"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             BackgroundColor="#25282C">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition />
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="32"/>
            <ColumnDefinition />
            <ColumnDefinition Width="32"/>
        </Grid.ColumnDefinitions>
        <VerticalStackLayout Grid.Row="1" Grid.Column="1">
        <Label Text="Make a game package"
               Style="{StaticResource Headline}"
               TextColor="White"
               SemanticProperties.HeadingLevel="Level1" 
               HorizontalOptions="Start"/>
        <Label HeightRequest="8" />
        <Label Text="Locate your game's build to create a package. The Package Tool will remember these paths based on the contentID for easier access in the future."
               HorizontalOptions="Start"
               TextColor="White"
               LineBreakMode="WordWrap"/>
        <Label HeightRequest="32" />

        <!-- Grid: 4 coluns, 2 rows?-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="32" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="42" />
                <ColumnDefinition Width="667"/>
                <ColumnDefinition Width="64" />
                <ColumnDefinition Width="408" />
            </Grid.ColumnDefinitions>

                <!--TODO: Image for the number 1-->
            <Border Grid.Row="0" Grid.Column="0"
                    WidthRequest="24"
                    HeightRequest="24"
                    StrokeShape="RoundRectangle 24"
                    VerticalOptions="Start"
                    Padding="2"
                    Stroke="#B8B4FF">
                <Label Text="1" TextColor="White"/>
            </Border>
            <VerticalStackLayout Grid.Row="0" Grid.Column="1">
                <Grid>
                    <!--Grid: 2 Columns, 5 Rows-->
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="476"/>
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="96"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" 
                           Text="Build location" 
                           Style="{StaticResource Headline}"
                           TextColor="White"
                           SemanticProperties.HeadingLevel="Level3" 
                           HorizontalOptions="Start"/>
                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Locate your game files to build. Add layout files and encryption keys under Additional Details. See docs for more information."
                           HorizontalOptions="Start"
                           TextColor="White"
                           LineBreakMode="WordWrap"/>
                    <Label Grid.Row="2" Grid.Column="0"
                           Text="Game path"
                           TextColor="White"
                           HorizontalOptions="Start" />
                    <Entry Grid.Row="3" Grid.Column="0" 
                           Text="{Binding GameDataPath,Mode=TwoWay}"
                           TextColor="#D2D2D3"
                           BackgroundColor="#323538"
                           Placeholder="Select or enter the game's location" 
                           IsSpellCheckEnabled="False"/>
                    <Button Grid.Row="3" Grid.Column="2"
                            Text="Browse"
                            Command="{Binding BrowseGameDataPathCommand}"
                            WidthRequest="90"
                            HeightRequest="32"
                            IsEnabled="True"/>
                </Grid>
                <toolkit:Expander>
                    <toolkit:Expander.Header>
                        <HorizontalStackLayout>
                            <!--TODO: Animation for the arrow-->
                            <Label Text="Additional details" 
                                   TextColor="White"/>
                        </HorizontalStackLayout>
                    </toolkit:Expander.Header>
                    <Grid>
                        <!-- Grid: 2 column, 5 rows-->
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="476"/>
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="96"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="Layout file/content map"
                               TextColor="White"
                               HorizontalOptions="Start"/>
                        <Entry Grid.Row="1" Grid.Column="0"
                               Text="{Binding MappingDataXmlPath,Mode=TwoWay}"
                               TextColor="#D2D2D3"
                               BackgroundColor="#323538"
                               Placeholder="Select or enter the content map location"
                               IsSpellCheckEnabled="False"/>
                        <Button Grid.Row="1" Grid.Column="2"
                                Text="Browse"
                                Command="{Binding BrowseMappingDataXmlPathCommand}"
                                WidthRequest="90"
                                HeightRequest="32"
                                IsEnabled="True"/>
                        <Label Grid.Row="2" Grid.Column="0"
                               Text="Xbox configuration file"
                               TextColor="White"
                               HorizontalOptions="Start"/>
                        <Entry Grid.Row="3" Grid.Column="0"
                               Text="{Binding GameConfigPath,Mode=TwoWay}"
                               TextColor="#D2D2D3"
                               BackgroundColor="#323538"
                               Placeholder="Select or enter the game config location"
                               IsSpellCheckEnabled="False"/>
                        <Button Grid.Row="3" Grid.Column="2"
                                Text="Browse"
                                Command="{Binding BrowseGameConfigPathCommand}"
                                WidthRequest="90"
                                HeightRequest="32"
                                IsEnabled="True"/>
                        <RadioButton Grid.Row="4" Grid.Column="0"
                                     IsChecked="{Binding EncryptEkbButton, Mode=TwoWay}">
                            <RadioButton.Content>
                                <VerticalStackLayout>
                                    <Label Text="Encrypt with a new Encryption Key Blob (EKB)"
                                           TextColor="White" 
                                           HorizontalOptions="Start"/>
                                    <Label Text="Initial package creation must be encrypted"
                                           TextColor="White"
                                           HorizontalOptions="Start" />
                                </VerticalStackLayout>
                            </RadioButton.Content>
                        </RadioButton>
                    </Grid>
                </toolkit:Expander>
            </VerticalStackLayout>
            <Border Grid.Row="2" Grid.Column="0"
                    WidthRequest="24"
                    HeightRequest="24"
                    StrokeShape="RoundRectangle 24"
                    VerticalOptions="Start"
                    Padding="3"
                    Stroke="#B8B4FF">
                <Label Text="2" TextColor="White"/>
            </Border>
            <VerticalStackLayout Grid.Row="2" Grid.Column="1">
                <Grid>
                    <!-- 4 rows, 2 columns -->
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="481"/>
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="96"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0"
                           Text="Package output location"
                           Style="{StaticResource Headline}"
                           TextColor="White"
                           SemanticProperties.HeadingLevel="Level3" 
                           HorizontalOptions="Start"/>
                    <Label Grid.Row="1" Grid.ColumnSpan="2"
                           Text="Select the package output location."
                           HorizontalOptions="Start"
                           TextColor="White"
                           LineBreakMode="WordWrap"/>
                    <Label Grid.Row="2" Grid.Column="0"
                            Text="Package path"
                            TextColor="White"
                            HorizontalOptions="Start"/>
                    <Entry Grid.Row="3" Grid.Column="0"
                            Text="{Binding PackageFilePath,Mode=TwoWay}"
                            TextColor="#D2D2D3"
                            BackgroundColor="#323538"
                            Placeholder="Select or enter the package output location"
                            IsSpellCheckEnabled="False"
                            WidthRequest="481"/>
                    <Button Grid.Row="3" Grid.Column="2"
                            Text="Browse"
                            WidthRequest="90"
                            HeightRequest="32"
                            Command="{Binding BrowsePackageOutputPathCommand}"/>
                </Grid>
                <toolkit:Expander>
                    <toolkit:Expander.Header>
                        <HorizontalStackLayout>
                            <!--TODO: Animation for the arrow-->
                            <Label Text="Additional details" TextColor="White"/>
                        </HorizontalStackLayout>
                    </toolkit:Expander.Header>
                    <Grid>
                        <!-- 2 Columns, 2 Rows -->
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="481"/>
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="96"/>
                            </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="SubVal path"
                               TextColor="White"
                               HorizontalOptions="Start"/>
                        <Entry Grid.Row="1" Grid.Column="0"
                               Text="{Binding SubValPath,Mode=TwoWay}"
                               TextColor="#D2D2D3"
                               BackgroundColor="#323538"
                               Placeholder="Select or enter the content map location"
                               IsSpellCheckEnabled="False" />
                        <Button Grid.Row="1" Grid.Column="2"
                                Text="Change"
                                Command="{Binding BrowseSubValPathCommand}"/>
                    </Grid>
                </toolkit:Expander>
            </VerticalStackLayout>
            <Frame Grid.Column="2" Grid.RowSpan="3" WidthRequest="1" BackgroundColor="#6B7584" />
            <VerticalStackLayout Grid.Column="4" Grid.Row="0">
                <Label Text="Package Preview"
                       Style="{StaticResource Headline}"
                       TextColor="White"
                       SemanticProperties.HeadingLevel="Level3" 
                       HorizontalOptions="Start"/>
                    <Label HeightRequest="12 "/>
                    <Border Stroke="#6B7584"
                        StrokeShape="RoundRectangle 6"
                        StrokeThickness="1"
                        HeightRequest="89"
                        Padding="6">
                    <Grid>
                        <!-- 2 columns, 4 rows-->
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="65"/>
                                <ColumnDefinition Width="14"/>
                                <ColumnDefinition Width="280"/>
                                <ColumnDefinition Width="34"/>
                            </Grid.ColumnDefinitions>
                        <!-- Image, sure, OK Copilot -->
                        <Image Grid.Row="0" Grid.Column="1" Grid.RowSpan="4"
                               Source="{Binding PackagePreviewImage}"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               HeightRequest="65"
                               WidthRequest="65"/>
                        <Label Grid.Row="0" Grid.Column="2"
                               HorizontalOptions="Start"
                               Text="{Binding PackageName}" 
                               TextColor="White"/>
                        <Label Grid.Row="1" Grid.Column="3" HorizontalOptions="Start">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="ID: "
                                          TextColor="White"/>
                                    <Span Text="{Binding BigId}"
                                          TextColor="White"/>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                        <Label Grid.Row="3" Grid.Column="3" HorizontalOptions="Start">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Size est: " 
                                          TextColor="White"/>
                                    <Span Text="{Binding PackageSize}" 
                                          TextColor="White"/>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </Grid>
                </Border>
            </VerticalStackLayout>
        </Grid>
        <HorizontalStackLayout HorizontalOptions="End">
            <Button Text="Cancel"
                    Command="{Binding CancelButtonCommand}" 
                    WidthRequest="90"
                    HeightRequest="32" />
            <Label WidthRequest="16" />
            <Button Text="Create package"
                    Command="{Binding MakePackageCommand}"
                    IsEnabled="{Binding IsMakePkgEnabled}" 
                    WidthRequest="128"
                    HeightRequest="34"/>
        </HorizontalStackLayout>
    </VerticalStackLayout>
    </Grid>
</ContentPage>